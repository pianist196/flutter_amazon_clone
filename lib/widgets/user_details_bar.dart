import 'package:flutter/material.dart';

class UserDetailsBar extends StatelessWidget {
  const UserDetailsBar({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
